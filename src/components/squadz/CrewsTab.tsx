import { ClubsTab } from "./ClubsTab";

/**
 * Crews = single unified group type (formerly Clans + Clubs).
 * Renders the full-featured group view (chat channels, members, owner).
 */
export function CrewsTab() {
  return <ClubsTab />;
}
