# animated

Drop-in folder for Framer Motion / 21st.dev style animated components.
Keep components self-contained and driven by props so they compose with
the app theme. Prefer `motion.div` with reduced-motion fallbacks.
