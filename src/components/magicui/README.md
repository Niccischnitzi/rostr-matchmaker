# magicui

Drop-in folder for advanced Tailwind + Framer Motion components
(e.g. from 21st.dev). Keep components self-contained and prefer
props over hard-coded values so they compose with the app theme.
